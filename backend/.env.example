# Server Configuration
# Use port 5000 to match frontend default; change if needed
PORT=5000
JWT_SECRET=your_jwt_secret_here

# Database Configuration
# Using SQLite local file by default
DATABASE_URL="file:./dev.db"

# Email Configuration (for sending emails)
EMAIL_HOST=smtp.ethereal.email
EMAIL_PORT=587
EMAIL_USER=test@example.com
EMAIL_PASS=password
EMAIL_FROM="Karm Baba CRM" <no-reply@karmbaba.com>

# Twilio / WhatsApp (optional)
# TWILIO_ACCOUNT_SID=your_account_sid
# TWILIO_AUTH_TOKEN=your_auth_token
# TWILIO_FROM=+1415XXXXXXX  # WhatsApp-enabled Twilio number (in E.164 format)